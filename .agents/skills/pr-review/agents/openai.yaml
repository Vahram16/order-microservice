interface:
  display_name: "PR Review"
  short_description: "Review a PR against its approved plan"
  default_prompt: "Use $pr-review to review this implementation against its approved placement, context, acceptance criteria, and deterministic evidence."
policy:
  allow_implicit_invocation: false
