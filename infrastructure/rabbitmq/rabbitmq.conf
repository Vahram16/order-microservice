# Broker-wide payload guard. Application queue length/TTL limits are declared by MassTransit.
max_message_size = 1048576

# Heartbeats accelerate disconnect detection without disabling RabbitMQ recovery behavior.
heartbeat = 30

# Dedicated Prometheus endpoint exposed by rabbitmq_prometheus.
prometheus.tcp.port = 15692
